using System;
using System.IO;

/*namespace tutorials
{
    class ReadWords
    {
       static void Main()
        {
            StreamReader file = new StreamReader("words.txt");
            string line = file.ReadLine();
            string[] elements = line.Split(' ');

            string theWord = (elements[0]);
            string ID = (elements[1]);
            int numberTimesLearned = Convert.ToInt32(elements[2]);
            bool hasCollected = Convert.ToBoolean(elements[3]);

            foreach (string line1 in elements)
            {
                Console.WriteLine(line);
            }
        }
    }
}
*/